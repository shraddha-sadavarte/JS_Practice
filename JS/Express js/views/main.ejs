<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="main w-full min-h-screen bg-zinc-900">
        <div class="form mx-4">
            <form action="/create" method="post">
                <input class="block px-4 py-2 bg-zinc-800 w-full rounded-md outline-none text-white" type="text" name="title" placeholder="Enter text here">
                <textarea class="block mt-2 px-4 py-2 resize-none bg-zinc-800 w-full rounded-md outline-none text-white" name="content" id="" cols="30" rows="10" placeholder="Enter more text here"></textarea>
                <button class="block bg-blue-500 text-white px-4 py-2 rounded mt-2" type="submit">Submit</button>
            </form>
        </div>

        <div class="tasks p-10 flex gap-3 flex-wrap p-10">
            <% if(files.length > 0){ %>
                <% files.forEach(function(file){ %>
                    <div class="task w-72 px-3 py-4 rounded bg-zinc-800 text-white">
                        <h3 class="text-lg font-semibold"><%= file %></h3>
                        <a href="#" class="text-blue-400 inline-block mt-4 underline">Read More...</a>
                    </div>
                <% }) %>
            <% } else {%> 
                       <h2 class="text-zinc-400">No tasks yet</h2>
                    <% } %>
        </div>
    </div>

    <script src="/javascripts/script.js"></script>
</body>
</html>